# secrets
.env
.env.*
!.env.example

# deps / build
node_modules/
dist/
.turbo/
*.tsbuildinfo

# resolver build artifact (regenerated via `pnpm build:catalog`)
packages/resolver/catalog-index.json

# session data
data/
# curated knowledge source data (read by build script, must be tracked)
!packages/knowledge/data/
!packages/knowledge/data/**
*.local

# os / editor
.DS_Store
